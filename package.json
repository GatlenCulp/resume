{
	"dependencies": {
		"jsonresume-theme-even": "^0.26.0",
		"resumed": "^6.1.0"
	},
	"scripts": {
		"resume:init": "resumed init",
		"resume:render": "resumed render --theme even",
		"resume:validate": "resumed validate"
	}
}
